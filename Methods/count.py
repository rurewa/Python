# Подсчитывает количество вхождений (количество найденных объектов)
s = 'Abracadabra';
print(s.count('a'));
print(s.count('ra'));
print(s.count('x')); # Вернёт 0
#print(s.count(a : b)); # Не выходит почему-то
